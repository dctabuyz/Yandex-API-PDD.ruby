$LOAD_PATH.unshift File.expand_path("../lib", __FILE__)

require 'YandexPDD'
 
task :build do

	system "gem build pdd.gemspec"
end
 
#task :release => :build do
#  system "gem push YandexPDD-#{Yandex::PDD::VERSION}"
#end
